apiVersion: v1
kind: Service
metadata:
  name: sis-kitnet-backend
spec:
  selector:
    app: sis-kitnet-backend
  ports:
    - name: http
      port: 8089
      targetPort: 8089
      nodePort: 30001
      protocol: TCP
  type: NodePort