# =====================
# Profile: PROD
# =====================

spring.datasource.url=jdbc:postgresql://db:5432/kitnet
spring.datasource.username=admin
spring.datasource.password=admin

server.port=8089
spring.jpa.show-sql=false
spring.jpa.generate-ddl=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false

# Pool de Conexoes (Hikari)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.max-lifetime=1200000

# Otimizacao de Performance do Hibernate
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true
spring.jpa.properties.hibernate.batch_versioned_data=true

# Query Cache
spring.jpa.properties.hibernate.cache.use_query_cache=false

# Estatistica do Hibernate (PROD: desabilitado)
spring.jpa.properties.hibernate.generate_statistics=false
spring.jpa.properties.hibernate.session.events.log=false

# Formatacao de SQL (PROD: desabilitado)
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.use_sql_comments=false

# Lazy loading optimization
spring.jpa.properties.hibernate.enable_lazy_load_no_trans=false

# Logging de Performance (PROD: desabilitado)
logging.level.org.hibernate.SQL=INFO
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=INFO
logging.level.org.hibernate.stat=INFO
logging.level.com.zaxxer.hikari=INFO

spring.jackson.deserialization.fail-on-unknown-properties=true
spring.jackson.date-format=dd/MM/yyyy
spring.mvc.pathmatch.matching-strategy=ANT_PATH_MATCHER

# Actuator
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=never
management.metrics.enable.jvm=true
management.metrics.enable.hikaricp=true
management.metrics.enable.jdbc=true